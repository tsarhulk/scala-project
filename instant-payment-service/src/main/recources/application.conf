database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10

    properties = {
      serverName = localhost
      portNumber = 5432
      databaseName = "instant-payment-service"
      user = "root"
      password = "root"
    }
  }
}